# Research/members/news data (lib/api.ts → backend/)

# Base URL of the BAY backend. Defaults to http://localhost:4000 when unset.
# API_URL=http://localhost:4000

# Shared secret for POST /api/revalidate — must match the backend's
# REVALIDATE_SECRET. Unset = the route rejects everything (closed by default).
# REVALIDATE_SECRET=

# Contact form delivery (app/api/contact/route.ts)

# Optional. Create at https://resend.com/api-keys.
# Leave it unset and the form falls back to FormSubmit, which needs no account
# and no key — but CONTACT_TO has to be activated once by clicking the link
# FormSubmit mails on the first submission.
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# Optional. Where form submissions land.
# CONTACT_TO=blockchainatyonsei@gmail.com

# Optional. Sender address. Resend's shared sender works before you verify a
# domain; once bay.example is verified in Resend, switch to that domain so mail
# stops landing in spam.
# CONTACT_FROM=BAY Website <noreply@yourdomain.com>
